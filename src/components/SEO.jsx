"use client";

// Legacy SPA component — SEO metadata is now fully handled by Next.js
// App Router via the `metadata` export in each app/*/page.jsx file.
// This stub is kept for backward compatibility with src/pages/ components
// that still render <SEO> — it intentionally renders nothing.
const SEO = () => null;

export default SEO;
